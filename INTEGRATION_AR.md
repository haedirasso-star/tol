# TOTV+ — دليل التشغيل والبناء (نظام أكواد التفعيل)

## ما الجديد في هذه النسخة

### 1) نظام أكواد التفعيل (الميزة الأساسية)
- الأدمن يُدخل بيانات سيرفر واحد → تتولّد **كودان تلقائياً**.
- المستخدم يُدخل الكود مرة واحدة → يُجلب السيرفر ويُحفظ محلياً وسحابياً.
- التفعيل يستغرق **ثوانٍ** بدل انتظار الأدمن.

### 2) إصلاح فقدان الاشتراك بعد تسجيل الخروج ★
كان زر «تسجيل الخروج» يستدعي `Sub.logout()` التي تكتب `plan = free`
في Firestore — أي أنها **تدمّر الاشتراك المدفوع**. تم استبدالها بـ:
- `Sub.endSession()` → تمسح الجلسة المحلية فقط، لا تلمس السحابة.
- `Sub.cancelSubscriptionPermanently()` → الإلغاء الفعلي (للإدارة فقط، غير مستدعاة من أي زر).
- قواعد Firestore الجديدة **تمنع المستخدم من مسح اشتراكه** حتى لو حاول.

### 3) إصلاحات إضافية
- مفاتيح `SharedPreferences` صارت مرتبطة بالـ `uid` (مع ترحيل تلقائي)
  → لا يرث حساب اشتراك حساب آخر على نفس الجهاز.
- `_clearLocal` صارت تمسح كل المفاتيح (host/tier/days/code كانت منسيّة).
- `_syncFromFirestore` لا تُسقط الحالة المحلية عند فشل الشبكة، والأحدث صلاحيةً يفوز.
- اشتراك VIP صار له نسخة احتياطية سحابية تحت `users/{uid}.vip`.
- استعادة الدخول صارت تشمل الهوست والكود والباقة وتحمّل المحتوى فوراً.

---

## ملفات جديدة / معدّلة

| الملف | الحالة |
|---|---|
| `lib/services/activation.dart` | **جديد** — محرّك `ActivationService` |
| `lib/ui/pages/redeem_code.dart` | **جديد** — واجهة إدخال الكود |
| `lib/main.dart` | أضيف `part` للملفين + إعادة محاولة الحفظ عند الإقلاع |
| `lib/core/constants.dart` | `applyFromCode` · `endSession` · مفاتيح uid · مزامنة آمنة |
| `lib/services/smart_content_service.dart` | استعادة كاملة عند الدخول + التقاط الكود |
| `lib/ui/pages/profile_player_header.dart` | زر الكود + `endSession` بدل `logout` |
| `lib/ui/pages/payment_sheet.dart` | خيار «لديك كود تفعيل؟» في شاشة الباقات |
| `firestore.rules` | **أُعيدت كتابتها بالكامل** |
| `firestore.indexes.json` | **جديد** |
| `firebase.json` | أضيفت أقسام firestore / storage / functions |
| `admin.html` | **جديد** — صفحة أدمن مطوّرة بقسم «أكواد التفعيل» |

---

## خطوات التشغيل

### 1) نشر قواعد Firestore  ← ابدأ بهذه
```bash
firebase deploy --only firestore:rules,firestore:indexes,storage
```
أو يدوياً: Firebase Console ← Firestore ← Rules ← الصق محتوى `firestore.rules` ← Publish.

> **مهم:** بدون نشر القواعد الجديدة، تفعيل الأكواد سيفشل بـ `permission-denied`
> لأن المجموعة `activation_codes` غير موجودة في القواعد القديمة.

### 2) رفع صفحة الأدمن
`admin.html` صفحة واحدة مستقلة — افتحها مباشرة في المتصفح، أو ارفعها:
```bash
firebase deploy --only hosting
```
سجّل الدخول بحساب `admin@totv.com` أو `haedirasso@gmail.com`
(أو أي بريد مضاف في مجموعة `admins`).

### 3) بناء التطبيق
```bash
flutter clean
flutter pub get
flutter analyze          # شغّله وأصلح أي تحذير قبل البناء
flutter build apk --release
# أو للنشر على المتجر:
flutter build appbundle --release
```

---

## كيف تُنشئ أكواداً (من صفحة الأدمن)

1. افتح **🔑 أكواد التفعيل** من الشريط الجانبي.
2. في بطاقة «➕ إضافة سيرفر وتوليد أكوادّه» أدخل:
   - **Host** — رابط السيرفر (يُضاف `http://` تلقائياً إن نسيته)
   - **Username** و **Password**
   - **الباقة** — شهري / 3 أشهر / سنوي (عدد الأيام يُملأ تلقائياً وقابل للتعديل)
   - **عدد الأكواد** — الافتراضي **2**
3. اضغط **🔑 توليد الأكواد** → تظهر نافذة بالأكواد،
   مع «📋 نسخ الكل» و«⬇ تنزيل TXT».
4. تُرسل الأكواد إلى الزبائن. عند تفعيل أي كود تُكتب بيانات المشترك داخله فوراً.

### توليد دفعة كبيرة
الصق عدة سيرفرات في بطاقة «📦 توليد دفعة»:
```
Host: http://server1.com:8080
Username: user1
Password: pass1

Host: http://server2.com:8080
Username: user2
Password: pass2
```
أو بصيغة CSV — سطر لكل سيرفر: `host,username,password`

### البحث عن مشترك
بطاقة «🔎 بحث عن مشترك (بالإيميل)» تعرض:
الباقة · الأيام المتبقية · تاريخ الانتهاء · السيرفر · كود التفعيل ·
الجهاز · آخر ظهور · وكل الأكواد التي استخدمها.

### إدارة الأكواد
| الزر | الوظيفة |
|---|---|
| 📋 نسخ | ينسخ الكود بصيغة `TOTV-XXXX-XXXX` |
| 👁 تفاصيل | كل بيانات الكود وصاحبه |
| ⏸ إيقاف | يمنع استخدام الكود (لا يؤثّر على من فعّله بالفعل) |
| ♻ إعادة تعيين | يحرّر الكود من مشتركه ليُستخدم من جديد |
| 🗑 حذف | حذف نهائي |
| ⬇ تصدير CSV | كل الأكواد بصيغة Excel (بترميز UTF-8) |

---

## ملاحظات أمنية مهمة

1. **توكن تلجرام** ما زال مكتوباً في الكود (`payment_sheet.dart:470`،
   `profile_player_header.dart:767`، `constants.dart` داخل `_SubscriptionPageState`).
   يُفضّل نقله إلى `app_config/secrets` وإرسال الرسائل عبر Cloud Function.
   القواعد الجديدة تحمي `app_config/secrets` من القراءة العامة.

2. **الأرقام السرية لكروت آسيا سيل** تُحفظ نصاً صريحاً في `orders`
   وتُرسل عبر تلجرام. يُنصح بنقلها إلى مجموعة مقصورة على الأدمن وحذفها بعد الاستخدام.

3. **ملفات التوقيع** (`android/key.properties` و`android/app/totv_keystore.jks`)
   موجودة في المشروع ومُدرجة في `.gitignore` — لا ترفعها إلى أي مستودع عام.

4. **`receipt_b64`** في `orders` يخزّن صور الإيصالات داخل مستند Firestore
   (حتى 900KB للمستند الواحد، والحد الأقصى 1MB). يُنصح بالاعتماد على
   `receipt_url` من Storage وحده وحذف الحقل.

---

## اختبار سريع بعد النشر

1. انشر القواعد.
2. من صفحة الأدمن ولّد كودين لسيرفر تعرف أنه يعمل.
3. في التطبيق: سجّل دخول ← الحساب ← **«لدي كود تفعيل»** ← الصق الكود ← تفعيل.
4. تحقّق أن المحتوى ظهر، وأن الكود صار «مُفعّل» في صفحة الأدمن ويحمل إيميلك.
5. **الاختبار الحاسم:** سجّل خروج ثم دخول بنفس الحساب —
   يجب أن يعود الاشتراك تلقائياً دون طلب الكود مرة أخرى.

---

# تحديث التجميل (النسخة الأخيرة)

## 1) شاشة «من يتابع الآن؟»
ملف جديد: `lib/ui/pages/profile_gate.dart`

تظهر بعد تسجيل الدخول **وعند كل فتح للتطبيق**. عند الضغط على أيقونة الحساب:
- يُشغَّل **صوت TOTV+ المميّز** (`assets/sounds/signature.wav`)
- البطاقة تنكمش قليلاً ثم تكبر (`easeOutBack`)
- حلقة ذهبية تنتشر من مركز الأيقونة للخارج
- توهّج ذهبي ينفجر ثم يهدأ
- بقية العناصر تتلاشى ثم انتقال ناعم للمحتوى

المدة الكاملة ‎1.35 ثانية، مضبوطة على إيقاع الصوت.

الأيقونة تعرض صورة حساب Google إن وُجدت، وإلا الحرف الأول من الاسم
بتدرّج ذهبي فوق شعار التطبيق الباهت.

## 2) الصوت المميّز — ملاحظة مهمة ★
الصوت في الفيديو الذي أرسلته هو **صوت Netflix «ta-dum»**، وهو
**علامة تجارية صوتية مسجّلة** مملوكة لـ Netflix. لا يمكن استخراجه ووضعه
في التطبيق — استخدامه يعرّضك لمطالبة قانونية وللحذف من متجر Google Play.

لذلك ألّفتُ لك **صوتاً أصلياً خاصاً بـ TOTV+** مركّباً من الصفر:
- نغمة باص عميقة (‎98Hz) تعطي الثقل والفخامة
- جرس أول عند ‎D4 ثم ارتفاع إلى ‎D5/A5 عند ‎0.30s (لحظة «الانطلاق»)
- بريق عالٍ خفيف + صدى لوحي (plate reverb) للاتساع
- المدة ‎2.0 ثانية، mono ‎44.1kHz، ذروة ‎-1dBFS بلا قصّ

ملكيته لك بالكامل، ويعطي نفس الإحساس الفخم بلا أي مخاطرة قانونية.
لتعديله لاحقاً: بدّل `assets/sounds/signature.wav` و`android/app/src/main/res/raw/signature.wav`.

## 3) بطاقة تلجرام الزجاجية
كلاس `TelegramGlassCard` (في نفس الملف) — تأثير **Glassmorphism** حقيقي
عبر `BackdropFilter` بضبابية ‎18px، حدود بيضاء شفافة، وظل أزرق خفيف.

تظهر في مكانين:
- أسفل شاشة «من يتابع الآن؟»
- في صفحة الحساب تحت زر كود التفعيل

النص: **«انضم إلى قناة تلجرام — مواعيد جميع المباريات وتحديثات التطبيق أولاً بأول»**

الرابط يُقرأ من `app_config/remote_config → telegram` (يمكنك تغييره من صفحة
الأدمن بلا تحديث للتطبيق)، ويرجع لـ `kTgChannel` إن كان فارغاً.

## 4) أيقونة التطبيق الجديدة
تم توليد **15 ملفاً** من الشعار الجديد بأعلى جودة ممكنة:

| الكثافة | ic_launcher | ic_launcher_round | ic_launcher_foreground |
|---|---|---|---|
| mdpi | 48 | 48 | 108 |
| hdpi | 72 | 72 | 162 |
| xhdpi | 96 | 96 | 216 |
| xxhdpi | 144 | 144 | 324 |
| xxxhdpi | 192 | 192 | 432 |

إضافةً إلى:
- `assets/images/logo.png` — شعار داخل التطبيق ‎512×512 (كان أصغر بكثير)
- `store/play_store_icon_512.png` — لرفعه على Google Play
- `store/icon_master_1024.png` — النسخة الرئيسية للاستخدام المستقبلي

**ما جرى على الصورة:**
1. إزالة الإطار الرمادي الفاتح من الحواف (كان يظهر كخط باهت حول الأيقونة)
2. تنظيف بكسلات الزوايا البيضاء واستبدالها بخلفية الشعار الداكنة
3. تحويلها إلى مربّع مثالي ‎(full-bleed) ليطبّق أندرويد قناعه الخاص
4. تكبير بـ LANCZOS + `UnsharpMask` (حدّة أقوى للأحجام الصغيرة)
5. `ic_launcher` بزوايا دائرية، `ic_launcher_round` دائرية كاملة
6. `ic_launcher_foreground` بشعار داخل ‎64% من الإطار (المنطقة الآمنة لـ Adaptive Icon)
7. `ic_launcher_background` تغيّر من `#000000` إلى `#0F0F10` ليطابق خلفية الشعار

> **بخصوص «رفع الجودة»:** الصورة الأصلية ‎683×681 بكسل. لا يمكن اختراع تفاصيل
> غير موجودة فيها، لكن هذا المقاس **يكفي تماماً** — أكبر أيقونة يحتاجها
> أندرويد هي ‎432px (adaptive foreground) و‎512px لمتجر Play، وكلاهما أصغر من
> أو قريب من المصدر. النتيجة حادّة ونظيفة على كل الأجهزة.
> إن حصلت لاحقاً على الشعار بصيغة **SVG أو PNG بمقاس ‎1024+**، أرسله وسأعيد
> التوليد بجودة أعلى.

## 5) إصلاح جانبي
`assets/videos/0320.mp4` كان يُستدعى في `actor_login.dart:1248` لكنه
**غير معرّف في `pubspec.yaml`** — أي أنه كان يرمي استثناءً وقت التشغيل.
تمّت إضافته الآن، فيعمل فيديو خلفية شاشة الدخول بشكل صحيح.

---

# تحديث v2 — الإصدار 2.0.0

## 1) رفع الإصدار

| المكان | من | إلى |
|---|---|---|
| `pubspec.yaml` | `1.0.0+1` | **`2.0.0+2`** |
| `AppVersion._version` | `1.0.0` | **`2.0.0`** |
| `AppVersion._build` | `1` | **`2`** |

### ★ لتظهر رسالة التحديث للمشتركين

رفع رقم الإصدار في التطبيق **لا يكفي وحده**. يجب أن تخبر الخادم بالحد الأدنى
المطلوب. من صفحة الأدمن ← **🚀 الإصدارات**، أو مباشرة في Firestore:

```
app_config/version
  min_version : 2          ← الحد الأدنى المقبول
  force_update: true       ← true = إجباري، false = اختياري
  store_url   : https://totv.app/download.html
  update_msg  : نسخة جديدة بتصميم كامل وأداء أسرع
```

التطبيق يقارن `AppVersion.major` (وهو الآن **2**) بـ `min_version`.
من يحمل النسخة القديمة (1) سيرى شاشة التحديث فوراً.

> **رتّب الخطوات:** انشر ملف APK الجديد أولاً، ثم غيّر `min_version` إلى 2.
> لو عكست الترتيب، سيُحجب المستخدمون قبل توفّر النسخة الجديدة.

## 2) الخلفية السوداء الكاملة

كانت اللوحة سوداء **دافئة** بمسحة عنبرية (`#0F0E0D`). أصبحت سوداء صافية:

| الطبقة | قبل | بعد |
|---|---|---|
| الخلفية | `#0F0E0D` | **`#000000`** |
| السطح | `#171614` | `#0A0A0B` |
| البطاقات | `#1F1D1B` | `#121213` |
| المرتفع | `#282522` | `#1A1A1C` |
| الحواف | `#2E2B27` | `#232326` |
| النص | `#F2EDEA` (دافئ) | `#F5F5F7` (محايد) |

استُبدل **22 لوناً** في كل الملفات، فلا تبقى طبقة واحدة بالمسحة القديمة.

**الفائدة العملية:** على شاشات OLED (معظم الهواتف الحديثة) البكسل الأسود
الصافي **ينطفئ فعلياً** — فتبدو الشاشة مطفأة، ويوفّر بطارية، ويظهر المحتوى
وحده بلا إطار رمادي حوله.

## 3) لون تلجرام

كان أزرق تلجرام `#229ED9` ينشز عن الهوية الذهبية. صار `C.telegram` ذهبياً
(`#F5A623`)، وبطاقة تلجرام كلها ذهبية. الأزرق الأصلي محفوظ باسم
`C.telegramRaw` إن احتجته لاحقاً.

## 4) واجهة الدخول الجديدة

أُعيد بناؤها بأسلوب منصّات البثّ الحديثة:

```
┌─────────────────────────┐
│                         │  ← مشهد بوسترات 52%
│        TOTV+            │     يذوب تدريجياً في الأسود
│   آلاف الأفلام...       │
├─────────────────────────┤
│ ▸ المتابعة مع Google    │  ← ذهبي ممتلئ (الإجراء الأساسي)
│ ▸ المتابعة بالبريد      │  ← أسود بحدّ رمادي
│ ── جديد على TOTV+؟ ──   │
│    إنشاء حساب جديد      │  ← نص ذهبي بلا إطار
│  شروط الاستخدام...      │
└─────────────────────────┘
```

**ما تغيّر تحديداً:**

- **حُذف الزجاج الشفاف.** كان `BackdropFilter` يُظهر البوسترات خلف اللوحة
  فيشتّت الانتباه، وهو من أثقل عمليات الرسم في Flutter. اللوحة الآن مسطّحة
  سوداء — أوضح وأسرع.
- **المشهد يتقلّص ذكياً** من 52% إلى 26% عند فتح لوحة المفاتيح أو الانتقال
  لنموذج البريد، فلا يُحجب الحقل الذي تكتب فيه.
- **زر واحد أساسي** بلون ممتلئ، والبقية ثانوية. كان هناك ثلاثة أزرار متساوية
  البروز فلا يعرف المستخدم أين يضغط.
- **حقول الإدخال** صارت سوداء `#0E0E10` بحدّ `#232326`، وتتوهّج بالذهب عند
  التركيز.
- **ضغطة الزر** تصغّره 2.5% — استجابة لمسية تجعل الواجهة تبدو حيّة.

## 5) إصلاح: شعار TOTV+ كان مقلوباً

في السياق العربي (RTL) كان النص `TOTV+` يُعرض **`+TOTV`** لأن علامة الجمع
محايدة الاتجاه فتنتقل لليسار. اكتشفته عند معاينة التصميم قبل التسليم.

أُضيف `textDirection: TextDirection.ltr` إلى **11 موضعاً** في كل الشاشات:
الدخول، السبلاش، الرئيسية، الحساب، الدفع، ولوحة الأدمن.

## 6) تحسينات أداء

| التغيير | الأثر |
|---|---|
| حذف `BackdropFilter` من لوحة الدخول | أثقل عملية رسم في الشاشة |
| حذف `_SocialBtn` غير المستخدم | كود أنظف، تحذير أقل في `analyze` |
| توحيد الألوان في ثوابت | لا إعادة إنشاء كائنات لون في كل بناء |

---

# أيقونة وشعار TOTV+ الجديد

## القرار: صورتان لا صورة واحدة

العمل الفني الجديد جميل لكنه **مزدحم بالتفاصيل**: حبّات فشار، شبكتا سلة،
سيارة فورمولا، ونص «TOTV PLUS». عند تصغيره إلى 48 بكسل (حجم أيقونة الشاشة
على أقل كثافة) تتحوّل كل هذه التفاصيل إلى بقعة غير مفهومة، والنص يختفي.

اختبرتُ الأمر فعلياً قبل التنفيذ: العمل الكامل عند 48px غير مقروء،
بينما **الحرفان TP** يبقيان واضحين وقويّين عند كل الأحجام.

لذلك:

| الاستخدام | الصورة | السبب |
|---|---|---|
| **أيقونة الشاشة** | الحرفان TP مقصوصان | يُقرأ عند 48px |
| **السبلاش** | العمل الكامل | مساحة كبيرة — التفاصيل تظهر |
| **شاشة الدخول** | العمل الكامل | لحظة العلامة الأولى |
| **العلامة المائية** | العمل الكامل بشفافية 5% | إحساس فخامة خلف المحتوى |
| **صورة الحساب البديلة** | TP مربّع | مساحة صغيرة |

## ما تم توليده

**15 أيقونة إطلاق** (الحرفان TP) — mdpi حتى xxxhdpi، بثلاث صيغ:
مربّعة بزوايا دائرية، ودائرية كاملة، وطبقة أمامية لـ Adaptive Icon.

**أربعة أصول داخل التطبيق:**

| الملف | المقاس | الاستخدام |
|---|---|---|
| `logo_full.png` | 1024 | السبلاش وشاشة الدخول |
| `logo.png` | 512 | شعار عام |
| `logo_mark.png` | 512 | TP مربّع للمساحات الصغيرة |
| `watermark.png` | 900 شفاف | العلامة المائية |

**أصول المتجر:** `store/play_store_icon_512.png` و `store/artwork_1024.png`.

خلفية الأيقونة التكيّفية صارت `#000000` لتطابق خلفية العمل الفني تماماً.

## العلامة المائية

كلاس `Watermark` جديد في `constants.dart` — لفّ أي شاشة به:

```dart
Watermark(
  opacity: 0.05,                 // 0.03 – 0.07 هو المدى المريح
  align: Alignment.bottomCenter,
  child: yourContent,
)
```

الخلفية السوداء في العمل الفني حُوّلت إلى شفافية متدرّجة حسب السطوع،
فيظهر الذهب وحده ولا يظهر مربّع أسود حول العلامة.

مطبّقة الآن في: شاشة «من يتابع الآن؟» وصفحة تفعيل الاشتراك.

> **لماذا 5% وليس أكثر؟** جرّبتُ ثلاث درجات على خلفية سوداء صافية.
> عند 3% تكاد تختفي، وعند 10% تنافس النص وتُتعب العين.
> ‏5% تُعطي الإحساس دون أن تُزاحم المحتوى.

## ملاحظة على السبلاش

استُبدل نص `TOTV+` المتحرّك بالشعار الفعلي، مع الاحتفاظ بحركة الظهور
والتوهّج الذهبي. إن تعذّر تحميل الصورة لأي سبب، يعود التطبيق للنص الذهبي
تلقائياً (`errorBuilder`) فلا تظهر شاشة فارغة أبداً.
