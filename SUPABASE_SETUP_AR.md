# TOTV+ — دليل تشغيل Supabase (خطوة بخطوة)

## لماذا انتقلنا؟

كان التطبيق يقرأ من Firestore **13 قراءة في كل فتح** — منها 5 قراءات لنفس
المستند `users/{uid}` من خمسة أماكن لا يعرف بعضها عن بعض. مع 22 ألف مستخدم
يعني ذلك ~850 ألف قراءة يومياً، والحد المجاني 50 ألفاً فقط. لهذا كانت الأكواد
تتوقف عن العمل عند المستخدمين.

**بعد التعديل:**

| | قبل | بعد |
|---|---|---|
| قراءات Firestore لكل فتح | 13 | **2** |
| قراءات نظام الاشتراك | عبر Firestore (محدودة) | Supabase — **غير محدودة** |
| التكلفة الشهرية | تنهار أو ‎$500+ | **‎$0** |

---

## الخطوات (10 دقائق)

### 1) شغّل ملفَّي SQL

Supabase → **SQL Editor** → **New query**:

1. الصق كامل `supabase/01_schema.sql` → **Run**
2. الصق كامل `supabase/02_admin_functions.sql` → **Run**

يجب أن تظهر `Success. No rows returned` في الحالتين.

### 2) أنشئ حساب الأدمن

Supabase → **Authentication** → **Users** → **Add user** → **Create new user**

- Email: `haedirasso@gmail.com` (نفس الإيميل الموجود في جدول `admins`)
- Password: اختر كلمة مرور قوية
- فعّل **Auto Confirm User** ✅

> إن أردت إيميلاً آخر، أضِفه أولاً في SQL Editor:
> ```sql
> insert into public.admins (email, role) values ('you@example.com','super');
> ```

### 3) انسخ المفتاحين

**Project URL** ← Settings → **Data API** → أعلى الصفحة
مثال: `https://abcdxyz.supabase.co`

**Publishable key** ← Settings → **API Keys** → قسم **Publishable key**
يبدأ بـ `sb_publishable_...` (أو `eyJ...` في المشاريع القديمة)

> 🛑 **لا تنسخ من قسم «Secret keys» أبداً.** المفتاح الذي يبدأ بـ `sb_secret_`
> يتجاوز كل الحماية — من يملكه يقرأ كل أكوادك ويحذف قاعدتك. لا يُستخدم في
> هذا المشروع إطلاقاً. إن سُرّب بالخطأ: API Keys → ⋮ → **Revoke** فوراً.
>
> التطبيق وصفحة الأدمن كلاهما يرفض العمل تلقائياً إن اكتشف مفتاحاً سرّياً.

### 4) ضعهما في مكانين

**أ) التطبيق** — افتح `lib/core/supa_config.dart`:

```dart
const String kSupaUrl       = 'https://abcdxyz.supabase.co';  // ← ضع رابطك
const String kSupaPublicKey = 'sb_publishable_...';           // ← ضع مفتاحك
```

**ب) صفحة الأدمن** — افتح `admin.html`، ابحث عن `SUPA_URL` (قرب نهاية الملف):

```javascript
const SUPA_URL = 'https://abcdxyz.supabase.co';
const SUPA_KEY = 'sb_publishable_...';
```

> مفتاح `Publishable` **آمن تماماً** في الاثنين — Supabase نفسه يذكر أسفله
> أنه قابل للمشاركة علناً. الحماية تأتي من RLS داخل القاعدة، لا من إخفاء
> المفتاح، تماماً كمفاتيح Firebase الحالية.

### 5) ابنِ التطبيق

```bash
flutter clean && flutter pub get && flutter analyze
flutter build apk --release
```

لا حاجة لأي حزمة جديدة — العميل مبني على `dio` الموجود أصلاً.

---

## كيف تولّد أكواداً الآن

1. افتح `admin.html` → **🔑 أكواد التفعيل**
2. أعلى الصفحة صندوق دخول Supabase — أدخل الإيميل وكلمة المرور من الخطوة 2
   (مرة واحدة فقط، تُحفظ الجلسة)
3. بطاقة **«➕ إضافة سيرفر وتوليد أكوادّه»**: Host + Username + Password + الباقة
4. **🔑 توليد الأكواد** → يظهر الكودان

---

## الحماية

| الجهة | ما تستطيعه |
|---|---|
| التطبيق (anon) | تنفيذ `redeem_code` و `get_subscription` فقط |
| التطبيق | **لا يستطيع** قراءة أو سرد أي كود — RLS يمنعه تماماً |
| الأدمن (مسجّل دخول) | كل شيء — توليد، بحث، إيقاف، حذف |
| المخمّن | 10 محاولات فاشلة ⇒ إيقاف 15 دقيقة تلقائياً |

التفعيل يجري داخل معاملة قاعدة بيانات بقفل `FOR UPDATE` — **يستحيل**
أن يُفعَّل كود واحد من جهازين، حتى لو ضغط ألف شخص في نفس الثانية.

---

## الطاقة الاستيعابية

| المورد | الحد المجاني | استهلاكك بـ 2000 مشترك/يوم |
|---|---|---|
| القراءات | **غير محدودة** | — |
| الكتابات | **غير محدودة** | — |
| حجم القاعدة | 500 ميغا | ~2 ميغا/يوم ⇒ سنة كاملة آمنة |
| الباندويث | 5 غيغا/شهر | ~1.5 غيغا |

القاعدة المجانية تتوقف بعد 7 أيام خمول — لن يحدث هذا مع نشاط يومي.

---

## ملاحظة أمنية صادقة

دالة `get_subscription(uid)` تعتمد على أن معرّف Firebase (28 حرفاً عشوائياً)
غير قابل للتخمين. هذا كافٍ عملياً، لكن الأقوى هو ربط Supabase بـ Firebase Auth
(Authentication → Third-Party Auth) لتصبح الهوية موقّعة تشفيرياً.
أخبرني إن أردت تفعيله لاحقاً.

---

## إن لم يعمل التفعيل

الرسائل الآن تعرض **السبب الحقيقي** بدل «تعذّر التفعيل»:

| الرسالة | المعنى | الحل |
|---|---|---|
| `SUPA_CFG` | لم تُضبط المفاتيح | راجع الخطوة 4 |
| `HTTP_401` / `HTTP_403` | المفتاح خاطئ | انسخ `anon public` مجدداً |
| `HTTP_404` | الدوال غير منشورة | أعد تشغيل ملفَّي SQL |
| هذا الكود غير موجود | الكود خطأ أو من قاعدة أخرى | ولّد كوداً جديداً |
| محاولات كثيرة خاطئة | كبح التخمين | انتظر 15 دقيقة |
