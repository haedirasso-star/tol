# إعدادات بناء تطبيق TOL
workflows:
  android-workflow:
    name: TOL Android Build
    max_build_duration: 30
    instance_type: mac_mini_m1
    environment:
      flutter: stable
    scripts:
      - name: Get Flutter packages
        script: flutter pub get
      - name: Build Android APK
        script: flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/**/app-release.apk
    publishing:
      email:
        recipients:
          - haedirasso-star@example.com # يمكنك تغييره لإيميلك لتلقي الرابط
