<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        LaunchTheme: شاشة الإقلاع
        - يجب أن يرث من Theme.AppCompat لضمان التوافق مع API 21-35
        - windowLightNavigationBar يتطلب API 27+
        - windowLayoutInDisplayCutoutMode يتطلب API 28+
    -->
    <style name="LaunchTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <!-- windowLightNavigationBar: API 27+ only, safe to include -->
        <item name="android:windowLightNavigationBar">false</item>
        <!-- Display cutout support: API 28+ -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <!--
        NormalTheme: الوضع الطبيعي بعد انتهاء splash
        نفس الإعدادات للحفاظ على التناسق البصري
    -->
    <style name="NormalTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

</resources>
